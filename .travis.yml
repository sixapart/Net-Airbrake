language: perl
perl:
  - "5.10"
  - "5.16"
  - "5.20"
before_install:
  - cpanm -n Devel::Cover::Report::Coveralls
script:
  - perl Build.PL && ./Build build && cover -test
after_success:
  - cover -report coveralls
